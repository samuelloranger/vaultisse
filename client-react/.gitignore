node_modules/
dist/
*.local
.vite/

# Generated by @tamagui/vite-plugin on every build: a cache of the resolved
# config, rebuilt from src/theme/tamagui.config.ts.
.tamagui/

# Generated by @tanstack/router-plugin from src/routes/**. Committed on
# purpose: the typed route tree is what makes `to="/..."` typecheck, and
# `tsc --noEmit` has to work on a fresh clone without a build step first.
!src/routeTree.gen.ts
.tanstack/
