{
  "name": "vaultisse",
  "version": "1.1.4",
  "private": true,
  "description": "Version anchor for the Vaultisse release/Docker pipeline. Not published; client and server are built and versioned independently.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/samuelloranger/vaultisse.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.5.13"
  },
  "scripts": {
    "lint": "biome lint server/src server/test",
    "lint:fix": "biome lint --write server/src server/test",
    "format": "biome format --write server/src server/test"
  }
}
